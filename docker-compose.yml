services:
  postgres:
    image: postgres:16
    container_name: bist100-ai-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: bist100_ai
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
